/** @type {import('tailwindcss').Config} */
export default {
  content: [
    './src/**/*.{js,ts,jsx,tsx,html}', // Scans all files in the 'src' directory
    './public/index.html', // Scans the index.html in the public folder
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
